const {
  User,
  UserLoginHistory,
} = require("./user");
const Otp = require("./otp");
const Currency = require("./currency");
const UserWallet = require("./userWallet");

module.exports = {
  User,
  UserLoginHistory,
  Otp,
  Currency,
  UserWallet
};
