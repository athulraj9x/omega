const {
  User,
  UserLoginHistory,
} = require("./user");
// const {
//   GeneralSetting
// } = require("./");
const { Banner } = require("./banner")

module.exports = {
  User,
  // GeneralSetting,
  Banner,
  UserLoginHistory,
};
